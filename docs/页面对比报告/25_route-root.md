# 路由说明：`/`

## 1. 定位

`/` 不是正式业务页面，而是兼容入口路由。

## 2. 为什么单独记录

虽然它不属于正式视觉页面，但会影响：

- 首次进入系统时的默认落点
- 登录后跳转是否一致
- 调试或分享链接时的入口稳定性

## 3. 对齐要求

- 它的跳转结果必须与 mock 预期入口一致。
- 不允许因为真实登录链路变化，把用户带到与 mock 不同的首屏语义。
- 只做兼容，不承载独立视觉设计。
