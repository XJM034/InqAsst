# 接口与 Mock 冲突：`/teacher/attendance/demo/roster`

## 1. 冲突等级

`低`

这是兼容路由，冲突主要在旧 demo 名册入口如何落到真实名册对象上。

## 2. mock 依赖的对象

mock 对这条路由的期待是：

- 从旧入口也能看到对应名单页

## 3. 当前接口实际使用的对象

当前真正的名册对象已经是：

- 基于 `courseId/courseSessionId` 的真实 session 名册对象

## 4. 具体冲突点

- 旧路由如果还指向 demo 语义，就会与真实 session 路由冲突。
- 如果没有真实映射关系，这条路由就无法正确落到目标课程。

## 5. 处理建议

- 这条兼容路由只能作为“旧入口 -> 真实 session 名册对象”的转换层。
- 不允许继续维持独立 demo 数据对象。
