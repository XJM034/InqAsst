# 接口与 Mock 冲突：`/role-select`

## 1. 冲突等级

`低`

这页的冲突主要是角色分发是否仍基于真实认证结果。

## 2. mock 依赖的对象

mock 角色选择本质上依赖本地角色映射。

## 3. 当前接口实际使用的对象

当前如果保留这条路由，就必须消费真实登录返回的角色和校区结果。

## 4. 具体冲突点

- 不能再按手机号或本地映射判断角色。
- 不能保留旧 mock 角色路由决策。

## 5. 处理建议

- 这条路由只允许作为真实认证后的分发层。
- 若实际不再需要显式角色选择，可保留兼容跳转，但跳转依据也必须是真实接口结果。
