# InqAsst 移动端开发计划 v1

更新日期：2026-04-02

## 1. 文档目的

本文档用于固化 InqAsst 第一版移动端开发方案，作为后续工程初始化、页面开发、接口联调和验收的统一依据。

本计划基于以下材料整理：

- [`design.pen`](/Users/minxian/Documents/alex_project/InqAsst/design.pen)
- [`设计文档_v1.9_开发指导.md`](/Users/minxian/Documents/alex_project/InqAsst/设计文档_v1.9_开发指导.md)
- Pencil 官方文档 [Design ↔ Code](https://docs.pencil.dev/design-and-code/design-to-code)
- shadcn/ui 官方安装与主题文档

## 2. 第一版目标

第一版目标不是做完整教务系统，而是先交付一个可运行的移动端业务闭环：

- 老师可通过手机号登录，查看当天课程并完成点名。
- 管理员可通过移动端查看点名总控、未到学生，并完成现场应急处理。
- 页面样式基于 `design.pen` 的设计变量与组件结构落地，不采用脱离设计源的随意实现。
- 前端先完成可演示版本，再接入真实认证、真实课程数据和真实点名接口。

第一版上线后，系统应优先解决“管理端尽早发现未到学生”的问题，而不是扩展更多非核心功能。

## 3. 范围定义

### 3.1 本期开发范围

本期只开发移动端 H5/PWA，包含两类角色：

- 老师端
- 管理端

老师端页面范围：

- 登录页
- 多角色选择页
- 老师首页
- 老师首页无课态
- 点名页
- 首次提交确认弹窗
- 我的页
- 退出登录确认

管理端页面范围：

- 管理首页
- 点名总控页
- 未到学生列表页
- 复制反馈态
- 应急设置相关页面
- 学生名单相关移动页面

### 3.2 本期不做

- Web 管理设置中心
- 查询功能
- 小程序版本
- 原生 App
- 离线优先能力
- 推送通知
- 复杂教务后台
- 批量总规则维护页面

## 4. 产品决策

### 4.1 应用形态

第一版采用一个应用按角色分流的方式，而不是老师端和管理端拆成两个独立前端项目。

固定路由结构：

- `/login`
- `/role-select`
- `/teacher/*`
- `/admin/*`

规则如下：

- 单角色账号登录后直接进入对应首页。
- 多角色账号登录后进入角色选择页。
- 角色切换由前端统一会话层处理。

### 4.2 交付顺序

交付节奏固定为：

1. 先完成前端页面、交互、状态流和 mock 数据。
2. 再对接真实短信验证码、真实课程数据和真实点名接口。

虽然先做前端，但第一版就必须把领域模型、接口契约和权限结构按真实业务设计清楚，避免后期重构。

### 4.3 部署方式

部署目标为自托管 Docker。

第一版前端工程必须满足以下要求：

- 本地开发可直接启动
- 构建产物可 Docker 化
- 环境变量可在运行时注入
- 后续接入内网或自有数据库时无需更换前端框架

## 5. 技术选型

### 5.1 核心技术栈

- 框架：Next.js App Router
- 语言：TypeScript
- 样式：Tailwind CSS
- 组件库：shadcn/ui
- 图标库：Lucide
- 测试：
  - Vitest
  - Testing Library
  - Playwright

### 5.2 选择原因

选择 Next.js App Router 的原因：

- 可用一个 React/TypeScript 工程同时承载页面、服务端接口和未来的登录态处理。
- 适合自托管 Docker 部署。
- 支持 Server Components、Route Handlers，便于未来接正式后端时做分层。

选择 shadcn/ui 的原因：

- 组件开源、可复制到本项目内部，适合做内部业务系统的长期维护。
- 与 Tailwind CSS、CSS variables 配合自然，便于承接 Pencil 的设计变量。
- 适合作为基础交互层，再往上封装业务组件。

## 6. 设计到代码实施方案

### 6.1 总体原则

前端页面实现必须围绕 `design.pen` 进行，不采用“看着原型大概写一个类似页面”的方式。

实现链路固定为：

1. 保留 `design.pen` 在仓库根目录。
2. 读取 `design.pen` 中的 variables。
3. 将 variables 同步到前端 `app/globals.css`。
4. 使用 shadcn/ui 基础组件承接交互。
5. 基于业务需求封装应用组件。
6. 再组合成页面。

### 6.2 Pencil Variables 同步方案

按 Pencil 官方文档的 `CSS Variables ↔ Pencil Variables` 方式处理。

同步目标分为两层：

- shadcn 语义变量
- Pencil 业务变量

必须保留并使用的语义变量包括：

- `--background`
- `--foreground`
- `--card`
- `--card-foreground`
- `--border`
- `--input`
- `--primary`
- `--primary-foreground`
- `--secondary`
- `--muted`
- `--ring`

必须保留并使用的业务变量包括：

- `--jp-accent`
- `--jp-bg`
- `--jp-border`
- `--jp-border-strong`
- `--jp-negative`
- `--jp-positive`
- `--jp-surface`
- `--jp-surface-muted`
- `--jp-text`
- `--jp-text-muted`
- `--jp-text-secondary`
- `--color-success`
- `--color-success-foreground`
- `--color-warning`
- `--color-warning-foreground`
- `--color-error`
- `--color-error-foreground`
- `--color-info`
- `--color-info-foreground`
- `--category-art`
- `--category-humanity`
- `--category-labor`
- `--category-sports`
- `--category-tech`

字体变量同步要求：

- `--font-primary`
- `--font-secondary`

圆角变量同步要求：

- `--radius-none`
- `--radius-pill`
- `--radius-m`

### 6.3 主题策略

`design.pen` 中包含 Light 和 Dark 主题定义。

本期策略：

- 保留 Light 和 Dark token 输出结构。
- 第一版正式验收只覆盖 Light 主题。
- Dark 主题不作为当前页面验收标准，但变量层不丢弃。

### 6.4 icon 策略

统一使用 Lucide，与 `design.pen` 中的 icon 选择保持一致。

## 7. shadcn/ui 配置方案

### 7.1 components.json 约定

项目初始化时创建 `components.json`，至少满足以下配置方向：

- `rsc: true`
- `tsx: true`
- `tailwind.css: "app/globals.css"`
- `tailwind.cssVariables: true`
- `baseColor: "neutral"`
- `iconLibrary: "lucide"`

### 7.2 目录分层

组件分为两层：

- `components/ui/*`
- `components/app/*`

其中：

- `components/ui/*` 直接承接 shadcn/ui 基础组件。
- `components/app/*` 承接业务组件，不允许把业务逻辑堆进 `components/ui/*`。

### 7.3 必选基础组件

初始化时至少纳入以下基础组件：

- `button`
- `card`
- `dialog`
- `drawer`
- `input`
- `input-otp`
- `label`
- `badge`
- `tabs`
- `select`
- `separator`
- `scroll-area`
- `sheet`

若后续发现管理端页面对某些组件有强依赖，可再增补，但第一版不追求“先把全套组件装完”。

## 8. 项目目录规划

建议目录结构如下：

```text
InqAsst/
├── app/
│   ├── globals.css
│   ├── layout.tsx
│   ├── page.tsx
│   ├── login/
│   ├── role-select/
│   ├── teacher/
│   └── admin/
├── components/
│   ├── ui/
│   └── app/
├── lib/
│   ├── design/
│   ├── domain/
│   ├── services/
│   ├── mocks/
│   ├── auth/
│   └── utils/
├── public/
├── design.pen
├── 设计文档_v1.9_开发指导.md
└── 移动端开发计划_v1.md
```

### 8.1 lib/design

用于存放与 `design.pen` 同步相关的内容：

- Pencil tokens 解析逻辑
- token 映射结果
- 设计常量

### 8.2 lib/domain

用于定义业务领域模型和规则计算，不允许页面直接在组件里拼业务结构。

### 8.3 lib/services

用于定义数据获取和提交接口，页面不得直接读取 mock 文件。

### 8.4 lib/mocks

用于第一版前端演示期的假数据和 mock service 实现。

## 9. 领域模型

第一版必须先定义清楚领域模型，再开始页面开发。

建议模型包括：

- `Role`
- `User`
- `TeacherProfile`
- `AdminProfile`
- `Course`
- `Student`
- `TeacherAssignment`
- `DailyRule`
- `DailyRuleAdjustment`
- `EmergencyAdjustment`
- `AttendanceSession`
- `AttendanceRecord`
- `AttendanceStatus`
- `UnarrivedReason`
- `CopyPayload`

### 9.1 核心业务规则

生效规则优先级固定为：

- 现场应急调整
- 当天规则调整
- 总规则

即：

`现场应急调整 > 当天规则调整 > 总规则`

所有老师端页面展示必须基于最终生效规则，而不是原始规则。

## 10. 页面规划

### 10.1 登录页

功能：

- 输入手机号
- 输入验证码
- 发起登录

组件建议：

- `Card`
- `Input`
- `InputOTP`
- `Label`
- `Button`

### 10.2 角色选择页

功能：

- 展示当前账号可进入角色
- 点击后进入老师端或管理端

组件建议：

- `Card`
- `Button`
- `Badge`

### 10.3 老师首页

功能：

- 今日课程列表
- 周选择器
- 代课任务展示
- 明日预告
- 无课态

组件建议：

- `Tabs`
- `Card`
- `Badge`
- `Separator`

页面要求：

- 有代课任务时必须以明显样式区分。
- 存在课程调整时必须给出明确提示文案。

### 10.4 老师点名页

功能：

- 展示点名时间窗口
- 展示学生网格
- 支持点击切换已到/未到
- 支持一键全到
- 实时统计
- 提交确认

组件建议：

- `Card`
- `Badge`
- `Button`
- `Dialog`
- `ScrollArea`

页面要求：

- 管理端修改过的学生使用高可见状态提示。
- 超时未提交时不可阻断提交，但必须明显提醒。

### 10.5 老师我的页

功能：

- 展示当前账号信息
- 退出登录

组件建议：

- `Card`
- `Button`
- `Dialog`

### 10.6 管理首页

功能：

- 今日摘要
- 进入点名总控
- 进入应急设置

组件建议：

- `Card`
- `Button`
- `Badge`

### 10.7 管理点名总控页

功能：

- 展示今日班级点名完成度
- 区分等待老师点名与已完成
- 快速进入未到学生列表

组件建议：

- `Card`
- `Badge`
- `Tabs`

### 10.8 未到学生列表页

功能：

- 展示未到学生
- 按适合管理员判断的维度组织
- 复制未到学生文本
- 修改未到状态

组件建议：

- `Card`
- `Button`
- `Badge`
- `Dialog`

### 10.9 应急设置页

功能：

- 修改单个班级的当前上课时间
- 为单个班级新增代课老师
- 修改代课老师手机号

组件建议：

- 优先 `Drawer`
- 必要时 `Dialog`
- `Select`
- `Input`
- `Button`

### 10.10 学生名单相关页面

功能：

- 学生名单查看
- 新增学生
- 批量导入页面壳子
- 编辑学生

第一版要求：

- 页面壳子和基础交互先完成
- 真正的数据导入与正式提交留待后续接口接入阶段

## 11. 业务组件规划

第一版建议封装的业务组件如下：

- `RoleSwitcher`
- `CourseCard`
- `SubstituteCourseCard`
- `AttendanceStudentCard`
- `StatusBanner`
- `AttendanceSummaryBar`
- `UnarrivedStudentList`
- `CopyFeedbackToast`
- `AdminEmergencyActionSheet`

封装原则：

- 通用交互由 `components/ui/*` 提供。
- 业务命名和业务状态由 `components/app/*` 承接。
- 页面应尽量由业务组件拼装，而不是直接堆大量原子组件。

## 12. 服务边界

第一版虽然用 mock 数据，但接口形状必须直接对齐未来正式后端。

建议 service 接口如下：

- `requestOtp(phone)`
- `verifyOtp(phone, code)`
- `getCurrentUser()`
- `switchRole(role)`
- `getTeacherHome(date)`
- `getAttendanceSession(sessionId)`
- `submitAttendance(payload)`
- `getAdminSummary(date)`
- `getAdminControlBoard(date)`
- `getUnarrivedStudents(date)`
- `updateStudentAttendanceStatus(payload)`
- `updateEmergencyTeacherPhone(payload)`
- `createSubstituteTeacher(payload)`
- `updateEmergencyCourseTime(payload)`

### 12.1 服务层约束

- 页面不得直接 import mock JSON。
- 页面统一通过 service 层读取数据。
- 后续替换正式接口时，不改变页面调用方式，只替换 service 实现。

## 13. 状态管理方案

第一版采用轻量状态管理，不引入全局状态库。

方案如下：

- 首屏数据由服务端组件获取
- 页面内部交互状态由客户端组件管理
- 会话与角色信息通过统一 auth/session 模块维护

本期不引入：

- Redux
- Zustand
- MobX

如果后续真实联调阶段状态复杂度明显上升，再单独评估是否增加全局状态层。

## 14. 开发阶段拆分

### Phase 0：工程初始化

目标：

- 初始化 Next.js App Router 工程
- 配置 Tailwind CSS
- 配置 shadcn/ui
- 建立 `components.json`
- 建立 `globals.css`
- 完成 Pencil variables 同步到前端 token
- 建立基础目录结构
- 建立 mock service 和领域模型
- 输出 Docker 运行方式

完成标准：

- 项目可本地启动
- 基础样式可用
- 可渲染一个示例页面

### Phase 1：老师端闭环

目标：

- 完成登录流程
- 完成角色选择
- 完成老师首页和无课态
- 完成点名页
- 完成首次提交确认
- 完成我的页和退出登录

完成标准：

- 老师端可从登录开始走完完整点名提交流程

### Phase 2：管理端闭环

目标：

- 完成管理首页
- 完成点名总控页
- 完成未到学生列表
- 完成复制文本反馈
- 完成未到状态修改入口

完成标准：

- 管理员可查看未到学生并完成基础现场总控动作

### Phase 3：应急设置与学生页面

目标：

- 完成单个班级上课时间调整
- 完成新增代课老师
- 完成修改代课老师手机号
- 完成学生名单查看、新增、编辑、批量导入壳子

完成标准：

- 管理端移动应急场景完整可演示

### Phase 4：真实接口接入准备

目标：

- 固化接口契约
- 固化错误处理方式
- 整理认证、课程、点名、应急设置的请求响应类型
- 为后续接短信验证码和真实课程数据做准备

完成标准：

- mock 与正式 API 的切换路径明确，不需要推倒页面重写

## 15. 测试计划

### 15.1 单元测试

覆盖以下核心逻辑：

- 规则优先级合并
- 今日生效课程生成
- 点名状态切换
- 未到学生复制文本生成
- 多角色账号分流逻辑

### 15.2 组件测试

覆盖以下业务组件：

- `CourseCard`
- `AttendanceStudentCard`
- `StatusBanner`
- `AdminEmergencyActionSheet`

验证重点：

- 状态变化是否正确
- 文案是否正确
- 是否正确消费 CSS variables

### 15.3 集成测试

覆盖以下场景：

- 登录到老师端首页
- 登录到管理端首页
- 老师完成点名提交
- 管理员复制未到学生文本
- 管理员修改代课老师手机号

### 15.4 E2E 测试

覆盖以下关键闭环：

- 老师手机号登录到完成点名
- 管理员登录到查看未到学生并执行应急修改
- 多角色账号进入角色选择并切换

### 15.5 视觉验收

视觉验收基准：

- [`design.pen`](/Users/minxian/Documents/alex_project/InqAsst/design.pen)

业务验收基准：

- [`设计文档_v1.9_开发指导.md`](/Users/minxian/Documents/alex_project/InqAsst/设计文档_v1.9_开发指导.md)

## 16. 验收标准

第一版验收标准如下：

- 用户可通过手机号登录并进入对应角色首页
- 多角色账号可正确选择角色
- 老师可查看今日课程并完成点名提交
- 点名页具备时间窗口提示、状态切换、统计和确认提交流程
- 管理员可查看班级点名完成度
- 管理员可查看未到学生并复制文本
- 管理员可修改未到状态
- 管理员可完成三类应急处理：
  - 修改单个班级当前上课时间
  - 新增代课老师
  - 修改代课老师手机号
- 页面视觉风格与 `design.pen` 保持一致
- 页面样式通过 Pencil variables + shadcn/ui 落地，而不是依赖散落的手写硬编码样式

## 17. 风险与默认决策

### 17.1 已确定默认决策

- 第一版只做移动端
- 一个应用按角色分流
- 前端先行，后端后接
- 最终认证方式是短信验证码
- 当前阶段允许使用 mock OTP 占位

### 17.2 主要风险

- 如果 mock 数据结构与真实后端字段偏离，后续联调成本会快速上升
- 如果页面开发阶段跳过 token 层，直接用零散样式实现，后续统一视觉会很痛苦
- 如果业务规则写在页面里，而不是沉淀到 domain/service 层，后续接真实接口会变得脆弱

### 17.3 风险应对

- 先定义领域模型和接口契约，再写页面
- 先完成 token 同步，再搭 UI
- 页面只消费 service，不直接碰 mock 数据

## 18. 下一步执行建议

建议从以下顺序开始执行：

1. 初始化 Next.js、Tailwind、shadcn/ui。
2. 从 `design.pen` 导出并接入 CSS variables。
3. 建立 `components/ui` 和 `components/app` 分层。
4. 先完成登录页、老师首页和点名页。
5. 再补管理首页、点名总控和未到学生列表。
6. 最后补管理端应急设置和学生页面壳子。

至此，第一版移动端开发计划定稿，可作为工程实施基线。
