# 本机 `daoleme` 联调工作流归档（2026-04-22）

## 归档原因

- 2026-04-22 起，当前项目默认测试口径收敛为“本机前端 + 已部署 shared dev 后端”。
- 因此，先前文档中把本机 `daoleme`、`127.0.0.1:8080`、Swagger 启动、`proxy/direct` 本机联调写成当前前置条件的内容，已不再适合作为主文档默认指引。
- 这些内容没有删除，只是从当前高频工作流里移出，保留为历史排障证据。

## 当前规则

- 默认不要启动本机 `daoleme` 作为测试前置。
- shared dev 暴露后端问题时，优先记录到 `docs/后端协同说明_20260414.md` 或新的 dated 文档。
- 只有在用户明确要求追溯历史本机问题、比对旧 benchmark、或让后端继续吸收旧修复时，才回看这份归档和相关历史章节。

## 这里保留的历史内容范围

- 旧版上线清单中的本机后端推荐环境、Swagger 检查、`127.0.0.1:8080` 启动方式
- 执行稿中的本机 `proxy/direct` 联调基线、dev RDS 说明、本机 smoke 结果与本机 benchmark 记录
- 后端协同说明中的本机预检 `OPTIONS /api/**` 返回 `500`、dev RDS 稳定性、macOS 本机编译、历史性能修复分支与 benchmark 记录

## 当前主入口

- 当前进度与默认验证口径：`docs/quality/`
- 发布门禁与人工写流：`docs/quality/regression-checklist.md`
- 后端协同事项与共享 dev 稳定性：`docs/后端协同说明_20260414.md`
