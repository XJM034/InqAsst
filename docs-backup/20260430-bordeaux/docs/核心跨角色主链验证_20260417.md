# 核心跨角色主链验证（2026-04-17）

## 1. 目标

验证这条最重要的业务链是否真实可用：

1. 管理端设置点名时间
2. 老师端在点名时间内完成点名
3. 点名结果同步到管理端
4. 管理端按行政班一键导出未到学生文本

## 2. 环境与样本

- 前端：
  - `http://127.0.0.1:3000`
- 后端：
  - `https://daoleme-dev.jxare.cn`
- 前端模式：
  - `proxy`
- 验证时间：
  - `2026-04-17 16:47 CST`

本轮实际样本：

- 校区：
  - `彭州市嘉祥外国语学校`
- 管理员：
  - `17748085601 / 王磊`
- 老师：
  - `19942378459 / 刘松林`
- 课程：
  - `3566 / 棍网球校队`
- 课节：
  - `4451`
  - `2026-04-17 17:00-18:30`

老师本轮提交为：

- 已到：
  - `9`
- 未到：
  - `2`
- 未到学生：
  - `周歆玥 / 小2023级4班`
  - `李一墨 / 小2024级1班`

## 3. 基线

验证前基线如下：

- `GET /api/courses/3566/attendance/status?courseSessionId=4451`
  - `hasSubmittedToday=false`
  - `unmarkedCount=11`
- `GET /api/courses/3566/roll-call-windows`
  - `[]`
- `GET /api/admin/roll-call/overview`
  - 目标课节 `rollCallCompleted=false`
  - `presentCount=0`
  - `absentCount=0`
- `GET /api/admin/absent-students?courseId=3566&courseSessionId=4451`
  - 11 条记录均为未提交前的空状态

## 4. 执行过程

### 4.1 管理端设置点名时间

当前后端真实可用口径不是“单课节点名时间覆盖”，而是“校区级点名规则”。

本轮实际生效写入：

- `PUT /api/admin/campuses/146/roll-call-rule`
- body：
  - `beforeStartMinutes=30`
  - `afterStartMinutes=90`

写入后：

- `GET /api/courses/3566/roll-call-windows`
  - 返回周一到周日 7 条窗口
  - 周五窗口对应：
    - `startOffsetMinutes=-30`
    - `endOffsetMinutes=90`

前端页面表现：

- `/admin/time-settings`
  - 能打开
  - 会显示 `当前：11:50-13:50`

注意：

- 这里显示的是“当天首条课节”的锚点，不是本次验证课节 `17:00-18:30` 的窗口文字
- 也就是说，这页当前表达的是“校区规则”，不是“当前正在验证的目标课节窗口”

### 4.2 老师端在窗口内完成点名

老师首页验证结果：

- `/teacher/home`
  - 目标课程 `棍网球校队` 出现
  - 主操作为 `开始点名`

老师点名页验证结果：

- `/teacher/attendance/demo?courseId=3566&courseSessionId=4451`
  - 页面展示点名窗口：
    - `16:30 - 18:30`
  - 页面展示草稿提示：
    - `当前页面展示的是老师端草稿，确认提交后才会同步到管理端。`
  - 提交前确认框正确列出 2 名未到学生及行政班

老师提交结果：

- `POST /api/courses/3566/attendance`
  - 提交成功
- `GET /api/courses/3566/attendance/latest?courseSessionId=4451`
  - `attendanceRecordId=35`
  - `hasSubmittedToday=true`
  - 提交时间：
    - `2026-04-17T16:47:08.074077`

### 4.3 管理端同步结果

同步后：

- `GET /api/admin/roll-call/overview`
  - 目标课节：
    - `rollCallCompleted=true`
    - `shouldAttendCount=11`
    - `presentCount=9`
    - `absentCount=2`
    - `progressPercent=82`
- `/admin/control`
  - 课程 `棍网球校队` 可见
  - 卡片显示：
    - `9/11 已到`

### 4.4 管理端未到学生与导出文本

同步后：

- `GET /api/admin/absent-students?courseId=3566&courseSessionId=4451`
  - 返回 2 条
  - 分别是：
    - `周歆玥 / 小2023级4班`
    - `李一墨 / 小2024级1班`
- `/admin/unarrived`
  - 页面可见这 2 名学生及对应行政班

复制文本验证：

- 点击：
  - `复制未到学生`
- 剪贴板文本包含：
  - `小2023级4班：周歆玥，棍网球校队`
  - `小2024级1班：李一墨，棍网球校队`

结论：

- “按行政班导出未到学生文本”这一步本轮验证通过

## 5. 本轮结论

这条核心跨角色业务链已经跑通：

1. 管理端设置点名时间
2. 老师端在时间窗内完成点名
3. 点名结果同步到管理端总控与未到学生
4. 管理端复制出的未到学生文本包含目标学生与行政班

但本轮同时发现 2 类问题：

- 后端问题：
  - 单课节点名时间覆盖接口当前不可用
  - 校区点名规则无法回滚到“无规则”
- 前端问题：
  - 老师点名页在可提交窗口内曾错误提示“超时不得提交”
  - 这项已在本地前端修复

## 6. 需要后端处理的问题

### 6.1 单课节点名时间覆盖接口不可用

实测：

- `PUT /api/admin/course-sessions/4451/roll-call-time`
- body：
  - `startOffsetMinutes=0`
  - `endOffsetMinutes=90`

返回：

- `code=1`
- `message=\"endTime is required; startTime is required\"`

同时：

- `DELETE /api/admin/course-sessions/4451/roll-call-time`

返回：

- `code=1`
- `message=\"session-level roll-call overrides are disabled; use the campus roll-call rule instead\"`

这说明：

- 前端契约里的“单课节 roll-call-time 写链路”当前不能真实使用
- 真实业务只能退回到校区级 `roll-call-rule`

### 6.2 校区点名规则缺少可用删除/回滚能力

实测：

- `DELETE /api/admin/campuses/146/roll-call-rule`

返回：

- HTTP `200`
- envelope：
  - `code=1`
  - `message=\"Server internal error\"`

影响：

- 无法把校区规则从“已配置”恢复到“无规则”
- 本轮只能做 best-effort 降级，不能彻底回到验证前基线

## 7. 本轮善后状态

由于删规则接口不可用，本轮只做了 best-effort 清理：

- 校区 `146 / 彭州市嘉祥外国语学校`
  - 点名规则从：
    - `beforeStartMinutes=30`
    - `afterStartMinutes=90`
  - 收紧为：
    - `beforeStartMinutes=0`
    - `afterStartMinutes=0`

当前遗留状态：

- 校区 `146` 仍不是“无规则”
- 目标课节 `4451` 已留下真实点名记录：
  - `attendanceRecordId=35`
  - `9 已到 / 2 未到`

如果后端补齐可用删除接口，建议后续把这两类测试残留清理掉。
