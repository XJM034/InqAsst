# 路由说明：`/role-select`

## 1. 定位

`/role-select` 是兼容/中转类路由，不是本次正式逐页视觉对齐的核心页面。

## 2. 为什么单独记录

mock 链路里角色落地比较直接，当前真实登录与校区选择引入后，这类中转路由更容易出现“还能打开，但语义已经偏了”的情况。

## 3. 对齐要求

- 保证角色选择或角色跳转逻辑与 mock 角色预期一致。
- 不把它做成比正式页面还重的中间页。
- 若当前实际已不需要显式角色选择，也要保留兼容跳转稳定性。
