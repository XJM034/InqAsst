# 到了么回归与发布门禁清单

更新日期：2026-04-29

## 内测期使用说明

- 项目已上线并进入内测期（用户于 2026-04-28 确认）。
- 本文件是内测期回归基线和发布前门禁留存，由原“上线前测试清单”续名而来。
- 内测反馈复现应优先记录真实环境、账号角色、页面路径、操作时间和接口表现。
- 本机前端 + shared dev 后端仍用于开发复现和诊断；不能直接替代内测 / 线上环境结论。

## 0. 推荐联调环境

当前推荐优先使用：

- 本机前端：`http://127.0.0.1:3000`
- 已部署后端：`https://daoleme-dev.jxare.cn`
- 请求模式：`proxy`

当前本机回归 / 发布验收口径：

```bash
PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000
E2E_LOGIN_CODE=8888
```

说明：

- 当前默认不要启动本机 `daoleme` 作为测试前置。
- `proxy` 是当前发布验收优先口径；`direct` 只保留给历史诊断背景，不作为当前要求。
- 若 shared dev 暴露后端问题，先记入 `docs/后端协同说明_20260414.md`。
- 若需要追溯历史本机 `daoleme` / dev RDS 联调、Swagger、`127.0.0.1:8080` 与旧 benchmark，请看：
  - `docs/archive/本机-daoleme-联调工作流归档_20260422.md`

## 1. 自动化门禁

发布和内测回归前，自动化门禁仍应全部通过：

```bash
npm test
npm run lint
npm run build
```

若本轮改动涉及站内导航、登录跳转、表单保存后回流、动态路由详情页或部署配置，还要额外确认：

- `npm run build` 后 `out/` 已更新，发布物以 `out/` 为准
- 不把 `next dev` 的客户端导航结果当成静态部署等价验证
- 在实际静态部署环境或等价静态产物环境复测，不出现 `?_rsc=` 或 `__next.*.txt` 404
- 重点页至少覆盖：
  - `/admin/home`
  - `/admin/control`
  - `/admin/time-settings`
  - `/admin/course-settings`
  - `/admin/me`
  - `/admin/emergency`
  - 本轮实际改动过的回流页与详情页

live smoke 由人工触发：

```bash
cp .env.e2e.example .env.e2e.local
# 填入真实手机号后执行
npm run test:e2e:smoke
```

可选身份断言环境变量：

```bash
E2E_TEACHER_EXPECTED_NAME
E2E_TEACHER_EXPECTED_CAMPUS
E2E_ADMIN_EXPECTED_NAME
E2E_ADMIN_EXPECTED_CAMPUS
E2E_MULTI_CAMPUS_ADMIN_EXPECTED_NAME
E2E_MULTI_CAMPUS_ADMIN_EXPECTED_CAMPUS
```

要求：

- 同一套环境变量连续通过 2 次
- 至少覆盖：
  - 教师账号
  - 单校区管理员账号
  - 多校区管理员账号
- 若后端已提供明确的联调账号清单，再补一轮“账号 / 角色 / 校区一致性矩阵”验证：
  - 对照 `/api/auth/login`
  - 对照 `/api/me` / `/api/admin/me`
  - 对照前端 cookie 与 `/teacher/me` / `/admin/me`
- 若要确认“无课 / 有课 / 今日课程数”是否显示正确，不能只看身份矩阵，还要额外对照当天业务接口：
  - 老师侧优先对照 `/api/teacher/home?weekStart=...` 与 `/api/teacher/courses/today`
  - 管理侧优先对照 `/api/admin/home/summary` 与 `/api/admin/roll-call/overview`
- 历史本机 `proxy` 复跑结果已归档，不作为当前默认测试要求
- 2026-04-15 已按新增身份断言口径复跑通过，实际使用：
  - 教师：`11211111111 / 白测试 / 成都高新云芯学校`
  - 管理员：`18512840290 / 岑曦 / 成都高新云芯学校`
  - 多校区管理员：`13888888888 / 邱邱 / 成都高新云芯学校`
- 2026-04-15 已补做一轮身份矩阵验证，结果 `7/7 PASS`：
  - `docs/账号角色校区一致性验证_20260415.md`

## 2. 自动化已覆盖的主链

- `/login`
- 教师登录、token 校验、首页主操作链路
- 管理员登录、选校区、首页、总控、未到学生、时间设置
- 当前不包含管理端跨日期切换专项；首页、总控、未到学生仍按默认今日口径覆盖
- 学生新增页加载
- 学生编辑页加载
- 当提供 `E2E_*_EXPECTED_NAME` / `E2E_*_EXPECTED_CAMPUS` 时，自动追加：
  - cookie 中的 `role / name / campus`
  - `/api/me` / `/api/admin/me`
  - `/teacher/me` / `/admin/me`
  的一致性断言
- 2026-04-15 已确认一条业务数据一致性样例：
  - 老师 `11211111111 / 白测试`
  - `/api/teacher/home?weekStart=2026-04-13` 返回 `weekCourses=[]`
  - `/api/teacher/courses/today` 返回 `[]`
  - 前端 `/teacher/home` 显示“今天暂无排课 / 无课”
  - 结论：当前无课显示与后端当天数据一致

说明：

- 以上自动化 smoke 全部是读路径验证
- 不在共享 dev 环境上自动提交任何写操作
- 现有自动化绿灯不能代替下面的发布前人工写流
- 2026-04-17 已补一条“已部署后端口径”的核心跨角色人工写流证据：
  - `docs/核心跨角色主链验证_20260417.md`

## 3. 发布前人工写流验收

### 3.1 教师端点名提交

- 使用教师账号 `18181459960`
- 进入今天有课的课程
- 完成一次点名提交
- 预期：
  - 页面提示提交成功
  - 名单状态更新
  - 刷新后状态保持一致

### 3.1.1 教师端点名：补录临时学生

- 使用今天有课的教师账号进入点名页
- 点击“补录临时学生”
- 输入学生姓名并选择行政班
- 保存成功后预期：
  - 页面不离开当前点名页
  - 新学生直接追加到当前名单，默认显示为“已到”
  - 老师可继续把该学生切换为“未到”
  - 提交点名时，新学生随本次点名结果一起上报
- 若是合班点名页，应在对应班级下补录，并确认最终提交 payload 仍带 `courseSessionId + studentId + status`
- 补录请求应调用 `POST /api/courses/{courseId}/students?courseSessionId={courseSessionId}`，确保代课 / 合班场景使用当前课节权限判断
- 若后端尚未支持该教师侧写接口，页面应显示可理解的失败提示，不得伪造添加成功

已验证记录：

- 2026-04-17
- 口径：
  - 本机前端 `3000`
  - 已部署后端 `https://daoleme-dev.jxare.cn`
- 管理员：
  - `17748085601 / 王磊 / 彭州市嘉祥外国语学校`
- 老师：
  - `19942378459 / 刘松林 / 彭州市嘉祥外国语学校`
- 课程：
  - `3566 / 棍网球校队`
- 课节：
  - `4451 / 2026-04-17 17:00-18:30`
- 老师实际提交：
  - `9 已到 / 2 未到`
- 未到学生：
  - `周歆玥 / 小2023级4班`
  - `李一墨 / 小2024级1班`
- 提交后已核对：
  - 教师端提交成功
  - 管理端总控同步为 `9/11 已到`
  - 管理端未到学生页出现上述 2 名学生
  - 复制未到学生文本包含对应“行政班：姓名，课程名”
- 详细证据：
  - `docs/核心跨角色主链验证_20260417.md`

### 3.1.2 管理端课程设置：编辑课程信息

- 使用管理员账号进入 `/admin/course-settings`
- 在“今日生效课程”卡片点击“编辑课程信息”
- 修改上课地点并保存
- 预期：
  - 正常进入编辑页
  - 首页课程卡片使用紧凑布局，左侧不再显示人物圆形图标，默认不显示“今日行课”徽标
  - 副标题单行显示地点、老师、人数，不显示上课时间
  - 页面可编辑上课地点
  - 保存后提示成功或返回后可见更新结果
  - 编辑页仍保留“移出今日行课”入口

### 3.1.3 管理端课程老师：紧凑列表展示

- 使用管理员账号进入 `/admin/course-teachers`
- 在任一有课程老师数据的星期下查看列表
- 预期：
  - 首屏慢请求期间先展示页面框架和列表骨架，不应长时间只停留在整页转圈
  - 单个老师卡片使用紧凑布局，不显示左侧人物占位
  - 老师姓名与手机号作为主信息展示
  - 课程名作为辅助标题展示
  - 副标题只显示地点，不显示上课时间
  - `代课老师` 仅在代课课程上显示特殊标识

### 3.1.4 管理端课程设置：添加空白课程

- 使用管理员账号进入 `/admin/course-settings/new-course`
- 切到“添加空白课程”
- 预期：
  - 页面不再手填开始时间 / 结束时间
  - 直接显示当天“时间设置”中的上课时间
  - 点名时间若已配置则直接显示，未配置则显示“未配置”
  - 学生区至少补 1 名学生，且支持系统内 / 系统外两种来源
  - 老师区支持系统内 / 系统外两种来源
  - 保存成功后，这门课直接进入“今日生效课程”
  - 若后端契约未接住，应返回可定位字段的错误，不应只返回泛化的“不能为null”

### 3.2 更换点名老师：选择已有老师

- 使用管理员账号 `18512840290` 或 `10000001112`
- 进入 `/admin/emergency`
- 若存在代课课程，顶部卡片标题显示“今日代课老师”
- 顶部代课卡片需突出显示当前代课老师姓名、手机号，并显示原上课老师
- 全部课程卡片使用紧凑布局，不显示上课时间，左侧不再显示人物圆形图标
- 选择一门当天课程
- 进入“更换点名老师”
- 选择一个已有老师并保存
- 预期：
  - 页面提交成功，不出现 `failed to fetch`
  - 返回课程详情页后，“当前点名老师”更新为新老师
  - 刷新后结果保持一致

补充说明：

- 当前候选列表允许同时出现：
  - 系统内老师
  - 已存在的系统外老师
- 若列表里已经有目标系统外老师，应该直接选择并保存，不需要再次录入姓名与手机号

已验证记录：

- 2026-04-15 使用 `18512840290`
- 课程：`/admin/emergency/course/988?courseSessionId=988`
- 已选择系统内老师：`alexxiang · 18140044661`
- 本轮提交成功，未再出现 `failed to fetch`

### 3.3 更换点名老师：恢复默认老师

- 在上一条已切换过老师的同一课程上
- 点击“恢复默认老师”
- 预期：
  - 页面提交成功
  - 当前点名老师恢复为默认老师
  - 刷新后结果保持一致

已验证记录：

- 2026-04-15 已在同一课程上执行恢复默认老师，恢复成功

### 3.3.1 更换点名老师：合班 / 互换二选一

- 适用前提：
  - 后端已提供合班创建能力
  - 目标系统内老师同校区、同日期、同时间段至少已有 `1` 节课
  - 前端契约涉及 `PUT /api/admin/roll-call-teacher/merge-group`、`GET /api/teacher/attendance/groups/{groupId}`、`POST /api/teacher/attendance/groups/{groupId}`、`GET /api/admin/roll-call/overview`
- 操作：
  - 在当前课节 `A` 进入“更换点名老师”
  - 选择目标系统内老师
  - 在确认弹窗里先选择“合班点名”或“互换点名老师”
- 预期：
  - 未选择处理方式前，确认按钮不可提交
  - 选择“合班点名”后，只展示合班课节多选；提交后 `A` 与所选课节形成一个合班任务，主点名老师为本次选择的目标老师
  - 选择“互换点名老师”后，只展示可互换课节单选；提交后仅 `A` 与所选课节交换点名老师，不创建合班
  - 老师端合班任务内部按班级分组，提交 payload 必须逐项带 `courseSessionId + studentId + status`
  - 管理端总控只显示一个合班任务卡，并展示班级数、老师、整体进度、未到 / 请假统计
  - 被合班课节的原点名老师只能查看点名情况，不显示提交主按钮

### 3.4 更换点名老师：录入系统外老师

- 在同一课程进入“录入系统外老师”
- 填写姓名与手机号并保存
- 预期：
  - 页面提交成功
  - 返回课程详情页后显示系统外老师已接管当前课节
  - 刷新后结果保持一致
  - 后续再次进入“更换点名老师”时，若后端候选范围允许，这名系统外老师应能继续作为可复用候选出现

说明：

- 本轮未自动执行这条写流
- 原因是当前没有删除外部老师入口，自动验证会向 dev RDS 留下不可自动回收的数据
- 这条仍保留为发布前人工阻塞项

### 3.5 管理端单个学生状态修改

- 使用管理员账号 `18512840290` 或 `10000001112`
- 进入总控页对应班级名单
- 修改 1 名学生状态
- 预期：
  - 页面立即反映状态变化
  - 返回列表或刷新后仍一致

### 3.6 管理端批量状态修改

- 在总控页选择多名学生
- 执行一次批量修改
- 预期：
  - 选中人数和最终状态一致
  - 刷新后仍一致

### 3.7 未到学生页状态修正

- 进入 `/admin/unarrived`
- 选取 1 名学生调整状态
- 预期：
  - 当前列表口径更新
  - 返回相关名单页后状态一致

### 3.8 时间设置修改与恢复

- 进入 `/admin/time-settings`
- 修改 1 个课节的时间设置
- 再恢复默认值
- 预期：
  - 修改后详情页和列表页同步更新
  - 恢复默认后口径回到原始状态

2026-04-17 实测补充：

- 本轮为了跑通核心主链，已在彭州校区实际写入点名规则
- 当前真实可用口径是：
  - `PUT /api/admin/campuses/{campusId}/roll-call-rule`
- 当前未通过项：
  - `PUT /api/admin/course-sessions/{courseSessionId}/roll-call-time` 不可用
  - `DELETE /api/admin/campuses/{campusId}/roll-call-rule` 不能恢复到“无规则”
- 因此这一项的“修改”已验证通过，但“恢复到原始空规则”当前仍是后端阻塞项
- 详细证据：
  - `docs/核心跨角色主链验证_20260417.md`

### 3.9 学生新增

- 进入某门课程的学生新增页
- 优先验证两条路径：
  - 输入至少 2 个字的学生姓名，确认会出现“当前校区已有学生”下拉匹配项
  - 点击已有学生后，姓名 / 学生ID / 行政班自动带出
- 若当前校区没有该学生：
  - 继续填写学生姓名
  - 选择行政班
  - 不再要求人工填写学生ID
  - 保存前会明确提示“将按临时学生创建，学生ID由数据库生成”
- 提交成功后返回学生名单
- 预期：
  - 学生出现在当前课程名单
  - 学生显示的学生ID与行政班正确
  - 选择已有学生时，名单里的学生ID应显示数据库 `studentId`
  - 临时新增学生时，保存成功提示与名单页都应能看到数据库分配的 `studentId`

注意：

- 建议优先使用测试账号或测试课程操作
- 目前没有删除接口，不要在共享 dev 上创建明显会污染业务数据的随机条目

### 3.10 学生编辑

- 进入某门课程的学生编辑页
- 修改：
  - 姓名
  - 或学生ID
  - 或行政班
- 提交保存
- 预期：
  - 返回名单后展示值更新
  - 重新进入编辑页后详情一致

### 3.11 多校区切换复核

- 使用多校区管理员账号 `13888888888`
- 登录后选择 `成都高新云芯学校`
- 查看首页和总控页
- 预期：
  - 页面数据属于所选校区
  - 切换流程不残留旧校区数据

### 3.12 核心跨角色主链

- 管理端设置点名时间
- 老师端在点名窗口内完成点名
- 管理端总控同步结果
- 管理端未到学生页同步结果
- 管理端复制未到学生文本
- 预期：
  - 老师提交结果与管理端总控 / 未到学生一致
  - 复制文本至少命中目标未到学生的“行政班 + 姓名 + 课程名”
  - 若存在写后回滚链路，测试后应恢复到原始基线

已验证记录：

- 2026-04-17 已按上述链路跑通一轮主闭环
- 通过项：
  - 主业务结果已贯通
  - 导出文本命中目标学生与行政班
- 未通过项：
  - 点名时间规则无法回滚到“无规则”
- 详细证据：
  - `docs/核心跨角色主链验证_20260417.md`

## 4. 当前明确不在上线范围

- 学生导入
- 课程设置首页汇总

若业务决定把它们纳入本轮上线，需要重新补测试计划和后端契约核对。
